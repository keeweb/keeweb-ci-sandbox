name: Build
on:
  push:
    branches: [ master ]
jobs:
  build-web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          repository: 'keeweb/keeweb'
      - name: Install npm modules
        run: npm ci
      - name: Grunt
        run: grunt --no-sign
